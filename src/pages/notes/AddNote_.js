import CustomPortalPage from '../../components/CustomPortal';
import AddNote from '../../components/notes/AddNote';

const AddNote_ = () => {
  return (
    <CustomPortalPage title="Record some new notes">
      <AddNote />
    </CustomPortalPage>
  )
};

export default AddNote_;
